# Migration `watch-20190715165209`

This migration has been generated by Harshit Pant at 7/15/2019, 4:52:09 PM.
You can check out the [state of the datamodel](./datamodel.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "file:dev"."EventType"("id" TEXT NOT NULL  ,"name" TEXT NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."PassType"("id" TEXT NOT NULL  ,"name" TEXT NOT NULL DEFAULT '' ,PRIMARY KEY ("id"));

CREATE TABLE "file:dev"."_EventTypeToPassType"("A" TEXT NOT NULL  REFERENCES EventType(id),"B" TEXT NOT NULL  REFERENCES PassType(id));

CREATE UNIQUE INDEX "file:dev"."EventType.id._UNIQUE" ON "EventType"("id")

CREATE UNIQUE INDEX "file:dev"."PassType.id._UNIQUE" ON "PassType"("id")
```

## Changes

```diff
diff --git datamodel.mdl datamodel.mdl
migration ..watch-20190715165209
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,27 @@
+datasource db {
+  provider = "sqlite"
+  url      = "file:dev.db"
+  default  = true
+}
+
+generator photon {
+  provider = "photonjs"
+}
+
+generator nexus_prisma {
+  provider = "nexus-prisma"
+  output   = "node_modules/@generated/nexus-prisma"
+}
+
+model EventType {
+  id               String     @default(cuid()) @id @unique
+  name             String
+  allowedPassTypes PassType[]
+}
+
+model PassType {
+  id                String      @default(cuid()) @id @unique
+  name              String
+  // passes                   Pass[]
+  allowedEventTypes EventType[]
+}
```

## Photon Usage

You can use a specific Photon built for this migration (watch-20190715165209)
in your `before` or `after` migration script like this:

```ts
import Photon from '@generated/photon/watch-20190715165209'

const photon = new Photon()

async function main() {
  const result = await photon.users()
  console.dir(result, { depth: null })
}

main()

```
